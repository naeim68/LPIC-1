# 🧩 GNU/Unix Commands — مدیریت فایل، لینک‌ها، پرمیژن، Wildcard/Regex

## 📂 مدیریت پوشه‌ها و فایل‌ها
| دستور | عملکرد | مثال |
|:--|:--|:--|
| `mkdir [name]` | ساخت دایرکتوری | `mkdir test` |
| `rmdir [name]` | حذف دایرکتوری خالی | `rmdir emptydir` |
| `rm -r [path]` | حذف بازگشتی | `rm -r oldproject` |
| `mkdir -p a/b/c` | ساخت تو در تو | `mkdir -p projects/devops/day1` |
| `touch [file]` | ایجاد/به‌روزرسانی زمان | `touch notes.txt` |
| `cat [file]` | نمایش محتوا | `cat notes.txt` |
| `echo "text"` | چاپ متن | `echo "Hello Linux"` |
| `echo "t" > f` | بازنویسی | `echo "new" > file.txt` |
| `echo "t" >> f` | افزودن به انتها | `echo "append" >> file.txt` |
| `ls` / `-a` / `-lh` / `-R` | لیست و جزئیات | `ls -lha` |

### ساختار خروجی نمونه‌ی `ls -l`
```bash
drwxr-xr-x 2 naeim staff 4096 Oct 6 09:42 projects
```
- کاراکتر اول: نوع فایل — `-` معمولی، `d` دایرکتوری، `l` لینک نمادین، `b` بلاک، `c` کاراکتری، `s` سوکت.

## 🔗 لینک‌ها
| نوع | توضیح | ساخت |
|:--|:--|:--|
| Hard Link | اشاره به inode اصلی | `ln file1.txt link1.txt` |
| Soft (Symbolic) Link | اشاره به مسیر | `ln -s file1.txt symlink1.txt` |

## ستون‌ها در `ls -l`
- Owner، Group Owner، Size، Date&Time، Name  
- جزئیات بیشتر: `stat file2.txt`

## سه نوع زمان فایل
- **atime** (دسترسی)، **mtime** (تغییر محتوا)، **ctime** (تغییر متادیتا)

---

# 🔐 مجوزها (Permissions) و مالکیت
نمونه: ``-rw-r--r--+``  (علامت `+` یعنی ACL دارد)

## سطوح دسترسی و نقش‌ها
- نقش‌ها: owner / group / other  
- دسترسی‌ها: `r` خواندن، `w` نوشتن، `x` اجرا (در پوشه، `x` یعنی اجازه‌ی ورود)

## مالکیت
```bash
chown user:group file.txt
chown :developer f1.txt
```

## تغییر مجوز با `chmod`
### Symbolic
```bash
chmod o+r file1
chmod g+w file1
chmod a+x file1
chmod o-x file1
chmod g=r file1
chmod u-x,g-w,o+r file1
```
### Numeric
- 4: r، 2: w، 1: x → مثال: `chmod 755 file1` ⇒ `rwxr-xr-x`

## بیت‌های خاص (SUID / SGID / Sticky)
| نوع | نماد | ستون | توضیح |
|:--|:--|:--|:--|
| SUID | s یا S | owner | اجرای فایل با مجوز مالک |
| SGID | s یا S | group | اجرای فایل/دایرکتوری با مجوز گروه |
| Sticky | t یا T | other | حذف محدود در پوشه‌های عمومی (مثل `/tmp`) |

مثال‌ها:
```bash
chmod u+s file1.txt
chmod g+s file1.txt
chmod o+t dir1
chmod 4755 script.sh
```

## umask (مجوز پیش‌فرض)
- فایل: پایه 0666، پوشه: 0777  
- مثال: `umask 0022` → فایل جدید 0644 ، پوشه 0755  
- تیمی: `umask 0002`

## انتقال پرمیژن از مرجع
```bash
chmod --reference=./d1 f*
```

---

# 🎯 Wildcard vs Regex

## Wildcard (در شِل و نام فایل‌ها)
- `*` هر رشته، `?` یک کاراکتر، `[abc]` یکی از این‌ها، `[!a-f]` غیر از بازه

نمونه فایل‌ها: `m, m2, man, mbn, mefghn, mz, s, s123e, s5, s6, sr567, mbz8, msaf500`

نمونه‌ها:
```bash
ls m*
ls s????         # دقیقاً 5 کاراکتر که با s شروع شود
ls m[c-f]*
ls m[!c-f][x-z]*
```
> Bash extglob: `shopt -s extglob` → `ls m?(ef|a|saf)*`

## Regex (در محتوا با grep/sed/awk)
```bash
grep "^m" file.txt         # شروع با m
grep "n$" file.txt         # پایان با n
grep "[0-9]" file.txt      # شامل رقم
grep -E "m[a-z]{2}" file.txt
grep -E "s(123|r567)" file.txt
```

## تفاوت‌ها
| ویژگی | Wildcard | Regex |
|:--|:--|:--|
| حوزه | نام فایل/مسیر | متن داخل فایل |
| تفسیر | Bash | ابزارها (grep/sed/awk) |
| پیچیدگی | ساده | قدرتمندتر |
