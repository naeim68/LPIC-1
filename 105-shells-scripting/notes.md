# 💻 آشنایی با شِل (Shell) و خط فرمان لینوکس

شِل (Shell) رابط متنی بین کاربر و سیستم‌عامل لینوکس است.  
با استفاده از آن می‌توانی دستورها را اجرا کنی، فایل‌ها را مدیریت کنی و کارهای سیستمی را خودکارسازی (Automation) انجام دهی.  
در مسیر DevOps، کار با شِل پایه‌ای‌ترین مهارت تو خواهد بود.

---

## 🧩 ساختار خط فرمان (Prompt)

```bash
root@ubuntuDesktop:~#
```

| بخش | توضیح |
|:--|:--|
| `root` | نام کاربر فعلی (مدیر سیستم) |
| `ubuntuDesktop` | نام میزبان (hostname) سیستم |
| `~` | مسیر فعلی (دایرکتوری Home) |
| `#` | نشانگر کاربر root |
| `$` | نشانگر کاربر معمولی |

> 💡 نکته: همیشه قبل از اجرای دستور با `#` مطمئن شو در مسیر درستی هستی؛ چون root می‌تواند کل سیستم را تغییر دهد.

---

## 🧭 دستورهای مرتبط با سیستم و مسیرها

| دستور | کاربرد | مثال |
|:--|:--|:--|
| `hostname` | نمایش نام سیستم | `hostname` |
| `hostnamectl set-hostname [newname]` | تغییر نام سیستم | `sudo hostnamectl set-hostname devops-lab` |
| `file [path]` | بررسی نوع فایل | `file /etc/passwd` |
| `pwd` | نمایش مسیر فعلی | `pwd` |

---

## 📘 دریافت راهنما و مستندات دستورها

| دستور | توضیح | مثال |
|:--|:--|:--|
| `man [command]` | نمایش مستندات کامل دستور | `man ls` |
| `[command] --help` | نمایش خلاصه راهنما | `ls --help` |

> 💡 می‌توانی در صفحه‌ی `man` با `/کلمه` جست‌وجو کنی تا بخش موردنظر را سریع پیدا کنی.

---

## ⚙️ ورودی و خروجی استاندارد (I/O Streams)

در لینوکس هر دستور سه مسیر ورودی/خروجی دارد:
- **stdin (0)** → ورودی (مثلاً از کیبورد)
- **stdout (1)** → خروجی معمولی
- **stderr (2)** → خروجی خطا

مثال:
```bash
ls /root >output.txt 2>error.txt
```
در این مثال:
- نتیجه‌ی درست (`stdout`) در فایل `output.txt` ذخیره می‌شود  
- خطاها (`stderr`) در فایل `error.txt` نوشته می‌شوند

---

## 🌍 مثال واقعی (در محیط DevOps)

هنگام کار روی سرورهای واقعی:
```bash
hostnamectl set-hostname web-prod-1
file /etc/hosts
pwd
```
با این دستورها مطمئن می‌شوی روی چه سروری هستی و در چه مسیری کار می‌کنی.

---

## 🧠 تمرین

1. نام سیستم خود را به `linux-lab` تغییر بده.  
2. دو فایل جدید بساز و با دستور `file` نوعشان را بررسی کن.  
3. خروجی دستورهای موفق و ناموفق را به دو فایل جداگانه هدایت کن (`stdout` و `stderr`).

---

## 🔗 مباحث مرتبط
- [الگوها و Regular Expression](./wildcards-vs-regex.md)
- [مجوزها و مالکیت فایل‌ها](../permissions/permissions-and-ownership.md)
